/**
 * @fileoverview Runtime environment values. Fill the Supabase fields to enable remote READ.
 */
export const environment = {
  /** True when the production build is served. */
  production: false,
  /** Supabase project URL. Leave empty to use local seed data. */
  supabaseUrl: '',
  /** Supabase anonymous key. Leave empty to use local seed data. */
  supabaseAnonKey: '',
};
